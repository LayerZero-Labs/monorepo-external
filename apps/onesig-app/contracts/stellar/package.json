{
    "name": "@layerzerolabs/onesig-stellar-contracts",
    "version": "0.0.0",
    "private": true,
    "description": "Stellar Soroban contracts for LayerZero OneSig protocol",
    "license": "GPL-3.0-only",
    "scripts": {
        "build": "pnpm package:source && pnpm exec lz-tool extra stellar verifiable-build --stellar-version 25.1.0 --archive .artifacts/.incoming/contracts-source.zip --package onesig:Cargo.toml",
        "build:contract": "pnpm resolve-dependencies && pnpm exec lz-tool stellar contract build --package onesig",
        "build:contract:native": "pnpm resolve-dependencies && pnpm exec lz-tool --docker-platform native stellar contract build --package onesig",
        "build:mocks": "pnpm resolve-dependencies && pnpm exec lz-tool --script \"cargo build --release --target wasm32v1-none -p mock_vault -p mock_chain -p mock_factory\" stellar",
        "build:mocks:native": "pnpm resolve-dependencies && pnpm exec lz-tool --docker-platform native --script \"cargo build --release --target wasm32v1-none -p mock_vault -p mock_chain -p mock_factory\" stellar",
        "build:native": "pnpm build:contract:native",
        "build:verifiable": "pnpm run build",
        "clean": "rm -rf ./dependencies ./target ./node_modules ./.artifacts*",
        "format": "pnpm exec lz-tool --script \"cargo fmt\" stellar",
        "format:check": "pnpm exec lz-tool --script \"cargo fmt -- --check\" stellar",
        "lint": "pnpm lint:rust",
        "lint:fix": "pnpm resolve-dependencies && pnpm exec lz-tool --script \"cargo clippy --all-targets -p onesig -p mock_vault -p mock_chain -p mock_factory --fix --allow-dirty --allow-staged && cargo fmt\" stellar",
        "lint:rust": "pnpm resolve-dependencies && pnpm exec lz-tool --script \"cargo fmt -- --check && cargo clippy --all-targets -p onesig -p mock_vault -p mock_chain -p mock_factory -- -D warnings\" stellar",
        "package:source": "pnpm resolve-dependencies && pnpm exec lz-tool extra stellar package-source --output .artifacts/.incoming/contracts-source.zip --exclude 'src/tests/**'",
        "resolve-dependencies": "pnpm exec build-utils-rust resolve",
        "test": "pnpm test:contract",
        "test:contract": "pnpm resolve-dependencies && pnpm exec lz-tool --script \"cargo nextest run --package onesig\" stellar"
    },
    "dependencies": {
        "@layerzerolabs/common-utils-macros-stellar-contracts": "workspace:*",
        "@layerzerolabs/common-utils-stellar-contracts": "workspace:*"
    },
    "devDependencies": {
        "@layerzerolabs/build-utils-rust": "workspace:*",
        "@layerzerolabs/vm-tooling-stellar": "workspace:*"
    },
    "publishConfig": {
        "access": "restricted",
        "registry": "https://registry.npmjs.org/"
    },
    "implicitDependencies": {
        "@layerzerolabs/common-utils-macros-stellar-contracts": "workspace:*",
        "@layerzerolabs/common-utils-stellar-contracts": "workspace:*"
    }
}
