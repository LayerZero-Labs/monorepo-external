# `@layerzerolabs/proxy-v5-6-evm-contracts`

OpenZeppelin v5.6 transparent proxy stack with two-step ownership: `TransparentUpgradeableProxy2Step` and `ProxyAdmin2Step`.

## Build

Compile with the options specified in `hardhat.config.js`.
