{
    "extends": ["//"],
    "tasks": {
        "build": {
            "inputs": ["$TURBO_DEFAULT$", "!**/*.md", "!**/LICENSE", "!src/tests/**"],
            "outputs": ["dependencies/**", ".artifacts/*.wasm", ".artifacts/*.zip"]
        },
        "test": {
            "dependsOn": ["build", "lint"]
        }
    }
}
